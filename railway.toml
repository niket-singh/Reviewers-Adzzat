[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on-failure"

# Enable Docker socket access for Project V image building
[deploy.docker]
socketAccess = true
