# Production Environment Variables
# ⚠️ IMPORTANT: Configure these in Azure Static Web Apps Portal
# Path: Azure Portal > Static Web Apps > Your App > Settings > Environment variables

# Backend API URL - Azure Container App
# Format: https://reviewers-backend-app.{unique-id}.{region}.azurecontainerapps.io/api
# Example: https://reviewers-backend-app.kindwave-12345678.eastus.azurecontainerapps.io/api
#
# To find your actual URL:
# 1. Go to Azure Portal > Container Apps > reviewers-backend-app
# 2. Copy the "Application URL" from Overview
# 3. Add "/api" at the end
# 4. Add this value in Static Web Apps > Configuration > Application settings
#
# If not set, defaults to: http://localhost:8080/api (for local testing)
NEXT_PUBLIC_API_URL=

# WebSocket URL (for real-time features)
# Format: wss://reviewers-backend-app.{unique-id}.{region}.azurecontainerapps.io/api/ws
# Same as API URL but with wss:// instead of https:// and /api/ws at the end
NEXT_PUBLIC_WS_URL=

# NOTE: For local development, use .env.local instead
# The .env.local file is already configured with localhost URLs
